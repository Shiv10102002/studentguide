import 'package:flutter/material.dart';

class NottificationsScreen extends StatelessWidget {
  const NottificationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Nottifications Screen"),
      ),
    );
  }
}
